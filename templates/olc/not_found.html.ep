%#<!DOCTYPE html>
% my $explanation
%   = $reason eq 'COURSE' ? "That course doesn't exist"
%   : $reason eq 'MODULE' ? "There is no such module in this course"
%   : $reason eq 'PAGE'   ? "There is no such page in this module"
%   : "";
<%= include 'parts/error_page',
    title   => "Course page not found",
    code    => 404,
    error   => "Page not found",
    message => <<"EOT"
I've looked everywhere and I just can't find the page
you've requested. $explanation
EOT
%>
