%#<!DOCTYPE html>
% $editable = $editable ? qq{ editablecomponentid="$editable"} : '';
<h2<%== $editable ? ' class="editable-component"' : '' %><%== $editable %>><%= $component->{heading_text} %></h2>
