%#<!DOCTYPE html>
% $editable = $editable ? qq{ editablecomponentid="$editable"} : '';
<div class="olc-paragraph<%= $editable ? ' editable-component' : '' %>"<%== $editable %>><%== $component->{paragraph_text} %></div>

