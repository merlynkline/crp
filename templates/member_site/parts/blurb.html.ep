%#<!DOCTYPE html>
<div class="instructor-photo"><img src="<%= $c->crp->url_for_instructor_photo(stash('site_profile')->instructor_id) %>" /></div>
<div class="yellow-text-edited"><%= b(stash('site_profile')->blurb) %></div>

